---
title: Call read-only function
description: Calls a read-only public function on a given smart contract.
toc: false
---

import { Root, API, APIExample } from '@/components/layout';
import { APIInfo, Property } from 'fumadocs-ui/components/api'
import { Badge } from '@/components/ui/badge';
import { Tabs, Tab } from 'fumadocs-ui/components/tabs'
import { Tabs as UITabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs';
import { Accordion, Accordions } from 'fumadocs-ui/components/accordion';

import { InlineCode } from '@/components/inline-code';

<Root>

<API>

<APIInfo method={"POST"} route={"/v2/contracts/call-read/{contract_address}/{contract_name}/{function_name}"}>

Call a read-only public function on a given smart contract.

The smart contract and function are specified using the URL path. The arguments and the simulated tx-sender are supplied via the POST body in the following JSON format:

### Request body

Map of arguments and the simulated tx-sender where sender is either a contract identifier or a normal Stacks address, and arguments is an array of hex serialized Clarity values.

<Property required={true} deprecated={false} name={"sender"} type={"string"}>

The simulated tx-sender

</Property>

<Property required={true} deprecated={false} name={"arguments"} type={"array of string"}>

An array of hex serialized Clarity values

</Property>

### Path parameters

<Property required={true} deprecated={false} name={"contract_address"} type={"string"}>

Stacks address

<span>Example:  `"SP187Y7NRSG3T9Z9WTSWNEN3XRV1YSJWS81C7JKV7"`</span>

</Property>

<Property required={true} deprecated={false} name={"contract_name"} type={"string"}>

Contract name

<span>Example:  `"imaginary-friends-zebras"`</span>

</Property>

<Property required={true} deprecated={false} name={"function_name"} type={"string"}>

Function name

<span>Example:  `"get-token-uri"`</span>

</Property>

### Query parameters

<Property required={false} deprecated={false} name={"tip"} type={"string"}>

The Stacks chain tip to query from

</Property>

| Status code | Description |
| :----------- | :----------- |
| `200` | Success |

</APIInfo>

<APIExample>

```bash title="Terminal"
curl -X POST "https://api.mainnet.hiro.so/v2/contracts/call-read/SP187Y7NRSG3T9Z9WTSWNEN3XRV1YSJWS81C7JKV7/imaginary-friends-zebras/get-token-uri" \
  -d '{
  "sender": "string",
  "arguments": [
    "string"
  ]
}'
```

<Tabs items={["200"]}>

<Tab value={"200"}>

GET request to get contract source

```json title="Example response"
{
  "okay": true,
  "result": "string",
  "cause": "string"
}
```

<Accordions>

<Accordion title={"Schema"}>

```ts
/**
 * GET request to get contract source
 */
export interface ReadOnlyFunctionSuccessResponse {
  okay: boolean;
  result?: string;
  cause?: string;
}

```

</Accordion>

</Accordions>

</Tab>

</Tabs>

</APIExample>

</API>

</Root>
